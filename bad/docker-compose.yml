version: '2'

services:
  docker-vulpy_latest:
    build: .
    volumes:
      - ./contrast_security.yaml:/app/bad/contrast_security.yaml
    container_name: vulpy-contrast
    ports:
      - "5002:5002"
